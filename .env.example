# aeo-infra environment configuration
# Copy to .env and customize for your environment

# =============================================================================
# User Configuration
# =============================================================================

# Default SSH username for all hosts
MESH_DEFAULT_USER=your-username

# =============================================================================
# Role Detection (mesh CLI)
# =============================================================================
# These determine which machine is the server vs clients.
# Comma-separated list of hostnames (case-insensitive).

# Hostnames that should be recognized as the Headscale server
MESH_SERVER_HOSTNAMES=myserver,myserver.local

# Hostnames for WSL2 clients (WSL2 and Windows often share a hostname)
MESH_WSL2_HOSTNAMES=myclient

# Hostnames for Windows clients
MESH_WINDOWS_HOSTNAMES=myclient

# =============================================================================
# Shared Folders
# =============================================================================

# Linux shared folder path
MESH_SHARED_FOLDER_LINUX=/opt/shared

# Windows shared folder path
MESH_SHARED_FOLDER_WINDOWS=C:\shared

# =============================================================================
# VM Configuration (vms/)
# =============================================================================

# VM storage directory
VM_STORAGE_DIR=/storage

# VM config directory
VM_CONFIG_DIR=/storage/vms

# VM bridge subnet (without last octet)
VM_SUBNET=192.168.50

# VM bridge gateway (host IP on bridge)
VM_GATEWAY=192.168.50.1
